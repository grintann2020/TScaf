namespace T {

    public enum EPrm : byte {
        Gm, // game
        Prgm, // program
        Stg,  // stage
        Scn, // scene
        Vw, // view
        UI, // user interface
        Intractn, // interactive
        Txt, // text

        Hb,
    }
}